# PulseChain RPC URL
PONDER_RPC_URL_369=https://rpc-pulsechain.g4mm4.io

# Database (Ponder uses Postgres in production)
DATABASE_URL=postgresql://user:password@localhost:5432/belt_indexer

# Database schema — each deployment uses a unique schema to avoid conflicts.
# Railway provides RAILWAY_DEPLOYMENT_ID automatically.
# The start script uses it: --schema $RAILWAY_DEPLOYMENT_ID --views-schema belt
# Views schema ("belt") stays stable so the dashboard can query a fixed target.

# Discord Webhook for notifications
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Ponder GraphQL endpoint (for notification script)
PONDER_GRAPHQL_URL=http://localhost:42069/graphql
